<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Evernot" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="main-layout">
      <% if authenticated? && !(params[:controller] == 'sessions' && params[:action] == 'new') %>
        <%= render "shared/sidebar" %>
      <% end %>
      <% if params[:controller] == 'notes' || (params[:controller] == 'notebooks' && params[:action] == 'show') %>
        <turbo-frame id="notes_panel" 
                     class="notes-panel" 
                     data-controller="notes-panel"
                     data-notes-panel-target="panel">
          <% if content_for?(:notes_panel) %>
            <%= yield :notes_panel %>
          <% else %>
            <% if params[:controller] == 'notes' && params[:action] == 'index' %>
              <%= render "shared/notes_panel", 
                        notes: current_user.notes.order(created_at: :desc), 
                        current: nil, 
                        notebook: nil %>
            <% else %>
              <%= render "shared/notes_panel", notes: [], current: nil, notebook: nil %>
            <% end %>
          <% end %>
        </turbo-frame>
      <% end %>
      <main class="main-content">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
